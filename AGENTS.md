# AGENTS.md

## 项目说明

本项目是南网 e 卡消息中心后端服务，当前由 AI Coding 辅助开发。

项目会长期迭代。任何开发任务必须以用户本次明确提出的需求为边界，不得根据模块名称、历史规划或个人判断提前开发后续功能。

## 技术栈

* Java 17
* Spring Boot 3.x
* Maven
* MyBatis-Plus
* 达梦数据库
* Redis
* RabbitMQ
* XXL-Job
* Knife4j / SpringDoc
* Jackson
* SLF4J + Logback
* Hutool

## 任务执行范围

1. 每次只实现用户当前明确要求的功能。
2. 开始修改前，只检查与当前任务直接相关的包、公共类和数据库脚本。
3. 除非任务明确要求，不扫描整个项目。
4. 不读取与当前任务无关的模块、需求文档和配置。
5. 不修改与当前任务无关的代码。
6. 不提前实现后续模块。
7. 不根据模块名称自行扩展功能。
8. 不主动进行额外重构、架构调整、性能优化或并发改造。
9. 发现无关问题时，只在完成说明中指出，不顺带修改。
10. 当前提示词指定了文件或包范围时，严格遵守指定范围。
11. 不访问当前后端工作区之外的前端或其他项目目录。
12. 不生成前端代码，除非用户明确要求。
13. 不生成临时 Demo 作为正式业务代码。
14. 不把 Prompt、需求文档和原型文件写入正式业务目录。
15. 不创建与当前需求无关的空类、空接口和占位架构。
16. 当前任务依赖尚未实现的模块时，优先保持业务边界；确有必要时，只定义最小且清晰的扩展接口，不完整实现依赖模块。

## 基础代码规范

1. 使用 Java 17、Spring Boot 3.x 和 Jakarta。
2. 不使用 `javax` 包。
3. 使用构造器注入，不使用字段注入。
4. 类名使用 PascalCase，变量和方法使用 camelCase。
5. 核心类和公共方法添加简洁中文 JavaDoc。
6. 注释应说明业务意图或设计原因，不重复代码表面含义。
7. 不给简单 getter、setter 和直观代码添加无意义注释。
8. 不使用 `System.out.println` 和 `e.printStackTrace()`。
9. 日志统一使用 SLF4J。
10. 不随意使用原始类型、无约束 `Object` 或未经说明的 `Map`。
11. 不在代码中写死生产地址、账号、密码、Token、密钥和证书。

## 分层规范

1. Controller 只负责接收请求、参数校验和返回结果。
2. Controller 不直接操作 Mapper。
3. Controller 不编写复杂业务逻辑。
4. Controller 不接收或返回数据库 Entity。
5. DTO 用于请求参数。
6. VO 用于响应结果。
7. Entity 只用于数据库映射。
8. Service 负责业务规则、流程编排、事务和状态流转。
9. Mapper 只负责数据访问。
10. 枚举、状态、类型和错误码统一管理，不散落硬编码。
11. 公共能力优先复用现有实现，不重复创建。
12. 复杂对象转换可集中放在 Converter 或 Assembler 中。

## 接口规范

1. 所有接口使用项目现有统一返回结构。
2. 所有业务异常走现有全局异常处理。
3. 请求参数使用 Jakarta Validation。
4. 分页查询复用现有统一分页对象和分页返回结构。
5. 接口字段以后端实际契约和当前需求为准，不自行改变字段含义。
6. 不为了方便前端而直接返回 Entity。
7. 接口新增或修改后，应保证 SpringDoc / OpenAPI 可以正确生成。
8. 不创建没有真实业务用途的空 Controller 或空接口。
9. HTTP 请求成功但业务可能失败时，应通过明确业务字段表达结果。

## ID 与状态规范

1. 数据库主键可以使用 `Long`。
2. Long 类型业务 ID 返回 JSON 时，必须遵守项目现有的 Long 转 String 规则。
3. 外部单位 ID、员工 ID 等标识始终使用 `String`。
4. 不得把外部字符串 ID 转换为数字。
5. `status` 使用数字：

    * `1` 表示启用
    * `0` 表示停用
6. 不使用 boolean 替代已经确定为数字状态的字段。
7. 数量、优先级、页码和 total 保持数字类型。
8. 查询条件中的合法 `0` 值不得被当作空值忽略。

## 数据库规范

1. Java 字段使用驼峰命名。
2. 数据库字段使用下划线命名。
3. 数据库使用达梦，不使用 MySQL 专属语法。
4. 业务表原则上包含创建时间、更新时间、创建人、更新人和删除标记。
5. 优先使用项目现有逻辑删除规范。
6. 自定义 SQL 必须明确处理逻辑删除条件。
7. 关联表需要反复删除重建时，可以按业务设计使用物理删除。
8. 状态和类型字段应有明确枚举或检查规则。
9. 高频查询字段根据真实查询场景建立索引。
10. 避免创建明显重复的索引。
11. 创建唯一约束前，必须确认是否与逻辑删除及删除后重建规则冲突。
12. 不在循环中逐条查询数据库，避免 N+1。
13. 批量统计优先一次查询并建立映射。
14. 多表写入、先删后插、复制、批量更新等操作必须放在事务中。
15. 任一步失败时整体回滚。
16. JSON 大文本使用达梦兼容的 CLOB。
17. 建表脚本必须包含必要的表注释和字段注释。
18. 数据库结构变更应同步更新项目现有达梦脚本。
19. 不自动执行数据库脚本，除非用户明确要求。

## JSON 与模板内容规范

1. JSON 序列化和反序列化统一使用项目现有 `ObjectMapper`。
2. 不手工拼接 JSON 字符串。
3. 结构化但字段动态的 JSON 使用 `JsonNode`。
4. JSON 解析、遍历和校验逻辑应集中封装。
5. 不使用普通字符串 `contains` 判断结构化 JSON 中的业务引用。
6. JSON 解析失败不得静默忽略。
7. 大文本内容应执行项目已有的大小、结构、节点数和深度限制。
8. 日志中不得输出完整模板内容、消息正文或敏感参数。

## 外部依赖规范

1. 未获得真实接口文档前，不实现短信网关、邮件服务、eLink 和员工中心的真实调用。
2. 不虚构外部 URL、鉴权方式、请求字段和响应结构。
3. 不返回模拟发送成功结果。
4. 不创建模拟员工信息。
5. 渠道与单位关联只保存外部 `unitId`，不为临时页面功能创建本地单位表。
6. 渠道匹配逻辑接收调用方提供的单位路径，不在匹配器内部写死组织树查询。
7. 外部服务后续通过独立 Client 或 Adapter 接入，不直接写入核心业务 Service。
8. 外部地址和凭证通过配置文件及环境变量提供。
9. 未明确要求时，不提前创建大量空 Client、DTO 和配置类。

## 日志与敏感信息

1. 日志只记录必要的业务标识、错误原因和耗时。
2. 不记录密码、Token、密钥和完整鉴权头。
3. 不完整记录手机号、邮箱和员工个人信息。
4. 不完整记录短信、邮件、模板和消息正文。
5. 异常日志保留必要堆栈，但返回前端的信息不得暴露内部实现。
6. 不在循环中输出大量无必要日志。

## 小型修改策略

对于字段调整、校验修正、接口小改和 SQL 约束调整：

1. 只检查直接相关的 Controller、Service、Mapper、DTO、VO、Entity 和脚本。
2. 不扫描所有业务模块。
3. 不重新分析完整项目架构。
4. 不进行与任务无关的性能、并发和安全改造。
5. 不生成额外分析文档。
6. 不编写临时自检脚本。
7. 不新增测试文件，除非用户明确要求。
8. 完成后只执行与改动规模匹配的验证。

## 测试与构建

默认不新增单元测试、集成测试或测试数据文件，除非用户明确要求。

不得为了通过验证：

1. 删除已有测试。
2. 跳过已有测试。
3. 降低已有校验规则。
4. 屏蔽编译错误。
5. 修改与当前任务无关的测试。

按任务规模执行验证：

### 小型代码修改

执行：

```bash
mvn -q -DskipTests compile
```

如果修改涉及 Service 业务逻辑、DTO、Mapper 或 SQL，再执行：

```bash
mvn test
```

### 新模块或较大业务修改

执行：

```bash
mvn clean package
```

`mvn clean package` 默认会运行现有测试，不需要再重复执行相同测试，除非用户明确要求分别执行。

### 环境联调

1. 有可用达梦数据库时，只验证与本次修改直接相关的接口。
2. 没有数据库环境时，明确说明未进行真实数据库和 HTTP 联调。
3. 缺少 Redis、RabbitMQ、XXL-Job 或外部服务时，明确说明未验证项。
4. 不得声称未实际执行的验证已经通过。
5. 不自动启动长期运行的服务，除非用户明确要求。

## 当前项目限制

1. 当前不开发登录认证系统。
2. 当前不开发用户、角色和权限系统。
3. 当前不开发完整组织机构管理。
4. 当前不引入完整若依项目。
5. 模块专属业务规则以当次提示词为准，不长期写入根目录 AGENTS.md。

## Git 与文档

1. 不自动执行 Git 提交。
2. 不自动推送远程仓库。
3. 除非用户明确要求，否则不更新 README。
4. 不自动生成大段开发说明文档。
5. 不修改与当前任务无关的配置、OpenAPI 或数据库脚本。

## 完成任务后的回复

完成后保持简洁，只说明：

1. 本次完成了什么。
2. 修改了哪些主要文件。
3. 是否新增或修改接口。
4. 是否新增配置、数据库表或字段。
5. 执行了哪些验证命令。
6. 构建和现有测试是否通过。
7. 是否存在确实未验证或需要用户确认的问题。

不要重复完整需求，不输出冗长的自我检查过程。

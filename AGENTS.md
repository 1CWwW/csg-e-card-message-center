# AGENTS.md

## 项目说明

本项目是后端服务项目，当前由 AI Coding 辅助开发。

项目会长期迭代，后续可能新增其他业务模块、基础能力或系统功能。AI 在任何开发任务中，都必须以用户本次明确给出的需求为唯一开发边界，不得根据猜测提前实现未被要求的功能。

## 技术栈

* Java 17
* Spring Boot 3.x
* Maven
* MyBatis-Plus
* 达梦数据库
* Redis
* RabbitMQ
* XXL-Job
* Knife4j
* Jackson
* SLF4J + Logback
* Hutool

## 开发边界

1. 每次只实现用户当前明确要求的模块或功能。
2. 不提前实现后续模块。
3. 不根据模块名称自行扩展额外功能。
4. 不修改与当前任务无关的代码。
5. 不重复创建已有公共能力，优先复用现有类。
6. 不破坏已有接口、配置、包结构和构建流程。
7. 不随意替换技术栈。
8. 不引入需求外的大型框架。
9. 不生成前端代码，除非用户明确要求。
10. 不生成临时 Demo 作为正式业务代码。
11. 不把 Prompt、需求文档、原型文件写入正式业务代码目录。
12. 如果当前任务依赖尚未实现的模块，只允许定义清晰的接口、占位适配类或 TODO 说明，不要擅自完整实现该依赖模块。

## 代码规范

1. 所有接口使用统一返回结构。
2. 所有异常走全局异常处理。
3. 所有分页查询使用统一分页对象。
4. Controller 只接收请求和返回结果，不写复杂业务逻辑。
5. Service 负责业务规则、事务和状态流转。
6. Mapper 只负责数据访问。
7. DTO 用于请求参数。
8. VO 用于响应结果。
9. Entity 用于数据库映射。
10. 枚举、状态、类型、错误码必须统一管理，不散落硬编码。
11. 不直接暴露 Entity 给前端。
12. 不在 Controller 中堆业务逻辑。
13. 不在业务代码中使用 `System.out.println`。
14. 不使用 `e.printStackTrace()`，异常必须通过日志框架记录。

## 注释规范

1. 核心类和核心方法必须添加简洁中文 JavaDoc。
2. 复杂业务规则、状态流转、事务、幂等、MQ、缓存、定时任务必须写清楚注释。
3. 不给简单 getter、setter 和一眼能看懂的代码添加废话注释。
4. 注释必须解释业务意图或设计原因，不要重复描述代码表面含义。

## 数据库规范

1. Java 字段使用驼峰命名。
2. 数据库字段使用下划线命名。
3. 业务表应包含创建时间、更新时间、创建人、更新人、删除标记。
4. 优先使用逻辑删除。
5. 状态字段、类型字段必须有明确枚举。
6. 高频查询字段应考虑索引。
7. 不在代码中写死数据库魔法值。
8. 数据库适配达梦数据库，不使用 MySQL 专属语法。

## 构建验证

每次修改后尽量执行：

```bash
mvn clean package
mvn test
```

如果构建或测试失败，需要修复直到通过。

## 完成任务后的回复要求

完成后简要说明：

1. 本次完成了什么。
2. 修改了哪些主要文件。
3. 是否新增接口。
4. 是否新增配置。
5. 是否新增数据库表或字段。
6. 是否已执行构建和测试。
7. 是否存在需要用户确认的问题。

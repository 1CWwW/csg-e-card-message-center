tsf_consul_ip: ${TSF_consul_ip:${TSF_CONSUL_IP:172.16.121.65}}
tsf_consul_port: ${TSF_consul_port:${TSF_CONSUL_PORT:8500}}
tsf_token: ${TSF_consul_token:${TSF_CONSUL_TOKEN:${CONSUL_TOKEN:d90be899-26c6-7fb7-84c3-68ba051611a8}}}
tsf_local_port: ${TSF_LOCAL_PORT:${server.port}}

tsf:
  service-registry:
    enabled: ${CONSUL_ENABLED:true}
    auto-registration:
      enabled: ${CONSUL_REGISTER:true}
  discovery:
    enabled: ${CONSUL_DISCOVERY_ENABLED:true}
    register: ${CONSUL_REGISTER:true}
    service-name: ${spring.application.name}
    port: ${tsf_local_port}
    health-check-path: ${server.servlet.context-path}/actuator/health
    health-check-interval: 10s
    register-health-check: true

spring:
  cloud:
    consul:
      enabled: ${CONSUL_ENABLED:true}
      host: ${tsf_consul_ip}
      port: ${tsf_consul_port}
      token: ${tsf_token}
      config:
        enabled: ${CONSUL_CONFIG_ENABLED:true}
      discovery:
        enabled: ${CONSUL_DISCOVERY_ENABLED:true}
        register: ${CONSUL_REGISTER:true}
        service-name: ${spring.application.name}
        health-check-path: ${server.servlet.context-path}/actuator/health
        health-check-interval: 10s
        register-health-check: true

feign:
  circuitbreaker:
    enabled: ${FEIGN_CIRCUITBREAKER_ENABLED:true}
